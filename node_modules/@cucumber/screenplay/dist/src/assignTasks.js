"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = __importDefault(require("path"));
function assignTasks(thisObj, tasksDir) {
    if (!tasksDir)
        throw new Error(`tasksDir was ${tasksDir}`);
    const files = (0, fs_1.readdirSync)(tasksDir);
    for (const file of files) {
        const match = file.match(/(\.ts|\.js|\.tsx|\.jsx)$/);
        if (match) {
            const ext = match[1];
            const name = path_1.default.basename(file, ext);
            Object.defineProperty(thisObj, name, {
                value: loadTask(name),
            });
        }
    }
    function loadTask(name) {
        const path = `${tasksDir}/${name}`;
        try {
            // eslint-disable-next-line @typescript-eslint/no-var-requires
            const task = require(path);
            return task[name];
        }
        catch (err) {
            return () => {
                throw new Error(`No task in: ${path}.{ts,js,tsx,jsx}`);
            };
        }
    }
}
exports.default = assignTasks;
